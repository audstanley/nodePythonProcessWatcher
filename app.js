let PythonShell = require('python-shell');

console.log("RUNNING")

//const processList = require('./process.json')

//let runningList = []

/*
for ([index, process] of processList.entries()) {
    console.log(process.process)
    let individualPythonProcess = new PythonShell(process.process)
    individualPythonProcess.on('message', (message) => {
        console.log(message)
    })

}
*/